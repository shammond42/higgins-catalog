%h2 Details
%dl.nav.nav-list
  - display_fields.each do |item|
    - if@artifact.send(item[:field]).present?
      %dt= item[:label]
      %dd= @artifact.send(item[:field])
= link_to_function "Return to Search", "history.back()", class: 'btn btn-inverse', style: 'width: 90%; margin-top: 10px'