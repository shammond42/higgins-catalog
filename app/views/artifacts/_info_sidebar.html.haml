%h2 Details
%dl.nav.nav-list
  - display_fields.each do |item|
    - if@artifact.send(item[:field]).present?
      %dt= item[:label]
      %dd= @artifact.send(item[:field])
= link_to "Return to Search", artifacts_path(session[:search_params]), class: 'btn btn-inverse', style: 'margin-top: 10px;'