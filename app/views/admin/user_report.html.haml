- admin_sidebar

%h1 Site Administrator List

%table.table.table-hover.table-condensed
  %thead
    %tr
      %th Name
      %th Title
      %th E-Mail
      %th Notifications
      %th Last Sign In
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.title
        %td= user.email
        %td= (user.receives_question_notifications? ? 'Yes' : 'No')
        %td= most_recent_sign_in(user)